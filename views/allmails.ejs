<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Messages</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f0f4f7;
            margin: 0;
            padding: 20px; /* Added padding for the top and left */
        }

        .container {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px; /* Wider container to show more messages */
            text-align: left; /* Align text to the left */
        }

        h1 {
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            background-color: #f9f9f9;
            margin: 8px 0;
            padding: 12px 15px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            font-size: 16px;
        }

        p {
            font-size: 16px;
            color: #666;
        }

        .logout-button {
            background-color: #ff4c4c; /* Red background */
            color: white; /* White text */
            border: none; /* No border */
            padding: 10px 15px; /* Padding */
            border-radius: 5px; /* Rounded corners */
            cursor: pointer; /* Pointer cursor on hover */
            font-size: 16px; /* Font size */
            position: absolute; /* Position the button */
            bottom: 20px; /* Distance from the bottom */
            left: 20px; /* Distance from the left */
        }

        /* Mobile adjustments */
        @media (max-width: 480px) {
            h1 {
                font-size: 20px;
            }

            li {
                font-size: 14px;
            }

            .container {
                padding: 20px;
            }

            .logout-button {
                font-size: 14px; /* Smaller font size for mobile */
                padding: 8px 12px; /* Smaller padding for mobile */
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>All messages <%= user.firstName %></h1>

        <% if (user.messages && user.messages.length > 0) { %>
            <ul>
                <% user.messages.forEach((message) => { %>
                    <li><%= message %></li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No messages to display.</p>
        <% } %>
    </div>
    
    <!-- Logout Button -->
    <form action="/user/logout" method="GET" style="position: absolute; bottom: 20px; left: 20px;">
        <button type="submit" class="logout-button">Logout</button>
    </form>
</body>
</html>
